<%= form_for(@tribute) do |f| %>
  <% if @tribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tribute.errors.count, "error") %> prohibited this tribute from being saved:</h2>

      <ul>
      <% @tribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Youtube URL" %><br>
    <%= f.text_field :url %>
  </div>
	<br/>
  <div class="actions">
    <%= f.submit %>
  </div>
	<br/>
<% end %>
